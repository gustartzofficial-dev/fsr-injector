#pragma once

// Bump this whenever a build is handed over, so the menu and the log can state
// exactly which revision is running. "It looks the same as before" is then a
// one-glance question instead of a file-size comparison.
#define FSRINJ_VERSION "0.5.2-dx11-state-fix"
